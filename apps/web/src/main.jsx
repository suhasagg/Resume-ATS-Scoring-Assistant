import React,{useState} from "react";import{createRoot}from"react-dom/client";import"./style.css";
function App(){const[resume,setResume]=useState("10 years Java distributed systems Kafka AWS engineer");const[out,setOut]=useState(null);async function run(){const r=await fetch("http://localhost:8080/api/score",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({job:{title:"Senior Backend Engineer",requiredSkills:["Java","distributed systems"],preferredSkills:["AWS","Kafka"],minimumYears:7},resumeText:resume})});setOut(await r.json())}return <main><h1>Resume / ATS Assistant</h1><p className="notice">Decision support only — human recruiter review is required.</p><textarea value={resume} onChange={e=>setResume(e.target.value)}/><button onClick={run}>Analyze alignment</button>{out&&<section><h2>Alignment: {out.result?.score}</h2><pre>{JSON.stringify(out,null,2)}</pre></section>}</main>}createRoot(document.getElementById("root")).render(<App/>);
